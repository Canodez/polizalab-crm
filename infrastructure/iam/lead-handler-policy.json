{
  "AWSTemplateFormatVersion": "2010-09-09",
  "Description": "PolizaLab CRM — Least-privilege IAM role and inline policy for the lead-handler Lambda. Grants full access to the Leads table (all GSIs), read access to the Clients table for duplicate detection during conversion, write access to the Clients table for creating a client on conversion, CloudWatch Logs, and X-Ray tracing.",

  "Parameters": {
    "Environment": {
      "Type": "String",
      "Default": "dev",
      "AllowedValues": ["dev", "stage", "prod"],
      "Description": "Deployment environment"
    }
  },

  "Resources": {
    "LeadHandlerRole": {
      "Type": "AWS::IAM::Role",
      "Properties": {
        "RoleName": {
          "Fn::Sub": "polizalab-lead-handler-role-${Environment}"
        },
        "Description": "Execution role for the polizalab-lead-handler Lambda function",

        "AssumeRolePolicyDocument": {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Principal": {
                "Service": "lambda.amazonaws.com"
              },
              "Action": "sts:AssumeRole"
            }
          ]
        },

        "Policies": [
          {
            "PolicyName": "LeadHandlerPolicy",
            "PolicyDocument": {
              "Version": "2012-10-17",
              "Statement": [

                {
                  "Sid": "LeadsTableReadWrite",
                  "Effect": "Allow",
                  "Action": [
                    "dynamodb:GetItem",
                    "dynamodb:PutItem",
                    "dynamodb:UpdateItem",
                    "dynamodb:DeleteItem",
                    "dynamodb:Query"
                  ],
                  "Resource": [
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Leads-${Environment}"
                    },
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Leads-${Environment}/index/userId-createdAt-index"
                    },
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Leads-${Environment}/index/tenantId-status-index"
                    },
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Leads-${Environment}/index/assignedTo-nextAction-index"
                    },
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Leads-${Environment}/index/phone-index"
                    }
                  ]
                },

                {
                  "Sid": "ClientsTableReadForDuplicateDetection",
                  "Effect": "Allow",
                  "Action": [
                    "dynamodb:GetItem",
                    "dynamodb:Query"
                  ],
                  "Resource": [
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Clients-${Environment}"
                    },
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Clients-${Environment}/index/phone-index"
                    },
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Clients-${Environment}/index/email-index"
                    }
                  ]
                },

                {
                  "Sid": "ClientsTableWriteForConversion",
                  "Effect": "Allow",
                  "Action": [
                    "dynamodb:PutItem",
                    "dynamodb:UpdateItem"
                  ],
                  "Resource": [
                    {
                      "Fn::Sub": "arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/Clients-${Environment}"
                    }
                  ]
                },

                {
                  "Sid": "CloudWatchLogs",
                  "Effect": "Allow",
                  "Action": [
                    "logs:CreateLogGroup",
                    "logs:CreateLogStream",
                    "logs:PutLogEvents"
                  ],
                  "Resource": {
                    "Fn::Sub": "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:log-group:/aws/lambda/polizalab-lead-handler-${Environment}:*"
                  }
                },

                {
                  "Sid": "XRayTracing",
                  "Effect": "Allow",
                  "Action": [
                    "xray:PutTraceSegments",
                    "xray:PutTelemetryRecords"
                  ],
                  "Resource": "*"
                }

              ]
            }
          }
        ],

        "Tags": [
          { "Key": "Project",     "Value": "PolizaLab" },
          { "Key": "Feature",     "Value": "Leads" },
          { "Key": "Environment", "Value": { "Ref": "Environment" } },
          { "Key": "ManagedBy",   "Value": "CloudFormation" }
        ]
      }
    }
  },

  "Outputs": {
    "RoleArn": {
      "Description": "ARN of the lead-handler IAM execution role",
      "Value": { "Fn::GetAtt": ["LeadHandlerRole", "Arn"] },
      "Export": {
        "Name": { "Fn::Sub": "PolizaLab-LeadHandlerRoleArn-${Environment}" }
      }
    },
    "RoleName": {
      "Description": "Name of the lead-handler IAM execution role",
      "Value": { "Ref": "LeadHandlerRole" },
      "Export": {
        "Name": { "Fn::Sub": "PolizaLab-LeadHandlerRoleName-${Environment}" }
      }
    }
  }
}
